# URL base del backend (sin slash al final)
# En desarrollo se usa el proxy de Vite (/api/v1), no es necesario configurar esta variable
# En producción (Railway) configurar con la URL real del backend
VITE_API_BASE_URL=https://greenpulse-back-production.up.railway.app/api/v1
